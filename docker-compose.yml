version: '3.8'

services:
  century-app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    env_file:
      - .env.local
    restart: unless-stopped
    container_name: century-dry-cleaner

networks:
  default:
    name: century-network
