# Century Dry Cleaner - User Guide

## 📦 What's in the Package

- `INSTALL_AND_RUN.bat` - Main launcher for the application
- `RUN_ME_FIRST.txt` - Quick start guide
- `docker-compose.yml` - Configuration for the application
- `Dockerfile` - Application setup instructions
- `data/` - Folder where all your data is stored

## 🚀 First-Time Setup

1. **Install Docker Desktop** (if not already installed):
   - Run `INSTALL_AND_RUN.bat`
   - If Docker isn't installed, it will open the download page
   - Install Docker Desktop and restart your computer

2. **Start the Application**:
   - Double-click `INSTALL_AND_RUN.bat`
   - A black window will appear - don't close it
   - Your default browser will open with the application

## 🔄 Daily Use

- **To start**: Just run `INSTALL_AND_RUN.bat`
- **To stop**: Close the black command window
- **Your data** is automatically saved in the `data` folder

## ⚠️ Important Notes

1. Keep the `data` folder safe - it contains all your information
2. Don't move or rename any files in the package
3. If you get an error about Docker not running:
   - Look for the Docker icon in your system tray (bottom right)
   - If it's not there, start Docker Desktop from the Start menu

## 📞 Need Help?

Contact support at: [Your Contact Information]
Include any error messages you see in the black window.
